<%- include('../partials/head') %>

</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">
<%- include('../partials/nav') %>

  <main class="w-full h-full">
    
    <div class="w-full relative">
      <img src="<%= home.photoUrl %>" alt="<%= home.houseName %>" class="w-full h-96 object-cover shadow-lg">
      <div class="absolute top-4 right-4 bg-red-500 text-white px-4 py-2 rounded-full font-semibold">Featured</div>
    </div>
    
    
    <div class="px-6 py-12 max-w-6xl mx-auto">
    
      <div class="mb-8">
        <h1 class="text-5xl font-bold text-gray-900 mb-3"><%= home.houseName %></h1>
        <div class="flex items-center gap-2 text-lg text-gray-600 mb-6">
          <span>📍</span>
          <span><%= home.location %></span>
          
        </div>
        
        <div class="flex justify-between items-center pb-8 border-b-2 border-gray-200">
          <span class="text-4xl font-bold text-blue-600">Rs.<%= home.price %></span>
          <div class="flex items-center gap-4">
            
            <%- include('../partials/add-to-favourite', { home }) %>


            
            <div class="flex items-center gap-2 bg-yellow-50 text-yellow-800 text-lg font-semibold px-6 py-3 rounded-lg border border-yellow-200">
              <span>⭐</span>
              <span><%= home.rating %>/5.0</span>
            </div>
          </div>
        </div>
      </div>

      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-10">
        <!-- Left Column -->
        <div class="space-y-6">
          <div class="bg-white rounded-lg shadow p-6">
            <h2 class="text-2xl font-bold text-gray-900 mb-4">About This Property</h2>
            <p class="text-gray-700 leading-relaxed">
              <%= home.description || "Beautiful and spacious property perfect for your next getaway." %>
            </p>
          </div>

          <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-xl font-bold text-gray-900 mb-4">🏠 Amenities</h3>
            <ul class="space-y-2 text-gray-700">
              <li class="flex items-center gap-2">✓ <span>Free WiFi</span></li>
              <li class="flex items-center gap-2">✓ <span>Kitchen</span></li>
              <li class="flex items-center gap-2">✓ <span>Parking</span></li>
              <li class="flex items-center gap-2">✓ <span>Air Conditioning</span></li>
            </ul>
          </div>
        </div>

  
        <div class="space-y-6">
          <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-xl font-bold text-gray-900 mb-4">📋 Details</h3>
            <div class="space-y-3 text-gray-700">
              <p><strong>Type:</strong> <%= home.type || "Apartment" %></p>
              <p><strong>Guests:</strong> <%= home.guests || "4" %></p>
              <p><strong>Bedrooms:</strong> <%= home.bedrooms || "2" %></p>
              <p><strong>Bathrooms:</strong> <%= home.bathrooms || "2" %></p>
            </div>
          </div>

          <button class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg shadow-lg transition duration-200">
            Book Now
          </button>
        </div>
      </div>
    </div>
  </main>

</body>
</html>
