<%- include('../partials/head') %>

</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">
<%- include('../partials/nav') %>

  <main class="py-4 px-4">
  <div class="flex flex-col items-center justify-center min-h-screen px-4">
    <div class="text-center">
      <h1 class="text-blue-600 text-3xl font-bold mb-4">All Host Homes</h1>
      <p class="text-md text-gray-600 mb-12">Let's Find Your Dream Home</p>
      
      <ul class="mt-8 flex flex-wrap gap-6 justify-center w-full max-w-6xl">
        <% homes.forEach(home => { %>
          <li class="bg-white rounded-lg shadow-lg hover:shadow-2xl transition-all duration-300 w-full sm:w-80 transform hover:scale-105">
            <img src="<%= home.photoUrl %>" alt="<%= home.houseName %>" class="w-full h-48 object-cover rounded-t-lg">
            <div class="p-4">
              <h2 class="text-lg font-semibold text-gray-800 mb-2"><%= home.houseName %></h2>
              <p class="text-sm font-bold text-gray-600 mb-3"><%= home.location %></p>
              <div class="flex justify-between items-center mb-3">
                <span class="text-xl font-bold text-blue-600">Rs.<%= home.price %></span>
                <span class="bg-yellow-100 text-yellow-800 text-sm font-semibold px-3 py-1 rounded">⭐ <%= home.rating %></span>
                
               
              </div>
              <a href="/host/edit-home/<%= home._id %>?editing=true"
                class="bg-green-500 hover:bg-green-700 text-white font-semibold px-4 py-2 rounded-lg transition-colors duration-200 w-full text-center block">Edit</a>
              </div>
              <form action="/host/delete-home/<%= home._id %>" method="POST" class="mt-2">
                <button class="bg-red-500 hover:bg-red-700 text-white font-semibold px-4 py-2 rounded-lg transition-colors duration-200 w-full text-center block">Delete</button>
              </form>


          </li>
        <% }); %>
      </ul>
    </div>
  </div>
  </main>
</body>
</html>